# 进度日志

## 2026-04-16

- 初始化项目根目录规划文件。
- 记录本轮目标：实现“帮我买”页面、表单校验、提交订单后跳转并展示进行中订单。
- 已读取 `mine.vue`、`pages.json`、帮我买参考图与提交后的进行中订单参考图。
- 已确认需要新增“帮我买”页面路由，并通过订单页状态控制实现提交后展示进行中订单。
- 已新增 `pages/buy/buy.vue` 并完成页面样式、交互和必填校验。
- 已更新 `mine.vue` 入口和 `pages.json` 路由。
- 已更新 `order.vue`，可在页面显示时读取下单结果并切换到“进行中”。
- 用户选择继续两项都做：补地址管理页，并把新订单同步到“我的订单”列表。
- 已新增 `pages/buy/address.vue`，完成地址独立选择与回填。
- 已更新 `buy.vue`，提交时同步写入订单详情缓存和订单列表缓存。
- 已更新 `order.vue`，列表态会合并展示用户新提交的“帮我买”订单。
- 已更新 `index.vue`，首页服务卡片存在选中态，“立即呼叫服务”按钮会按当前选中服务执行跳转。
- 已新增 `帮我送 / 帮我办 / 万能帮` 三个页面，并接入首页卡片和首页主按钮跳转。
- 已确认三个新服务页可复用 `buy.vue` 的订单缓存结构，订单页无需额外改动大框架即可接收。
- 已完成三个服务页的表单校验、按钮禁用、订单缓存写入与提交后跳转订单页联动。
- 已新增统一订单仓库 `utils/order-store.js`，四类订单提交现统一写仓库。
- 已重构 `pages/order/detail.vue`，按订单类型展示不同详情内容，并保留本地持久化字段。
