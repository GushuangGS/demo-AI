# 进度日志

## 2026-04-16

- 接收任务：将项目中的 Material Symbols 图标改为本地 PNG 资源。
- 已扫描到项目中存在大量模板图标和动态图标字段。
- 已确认本机可用 `sips` 将 SVG 转为 PNG。
- 已先新增 `LocalIcon` 组件，为后续统一替换做准备。
- 已完成全部模板图标替换，生成 61 个本地 PNG 图标，并移除运行时字体图标依赖。
- 已将 `LocalIcon` 从 `mask-image` 方案调整为 `<image>` 方案，修复页面图标不显示问题。
- 已进一步改为 `import.meta.glob()` 静态映射本地图标资源，修复小程序端动态路径可能不显示的问题。
- 已进一步改为显式 `import` 图标表方案，避免 `glob` 在当前构建链路下的潜在兼容问题。
